This folder contains filter scripts. They are intended to be used as filters
from vim (through the '!' command). That means that they have to read a range
from STDIN, modify it as necessary, and write the result to STDOUT.
